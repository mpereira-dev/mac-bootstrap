#!/usr/bin/env node
import { parseArgs } from "../src/args.js";
import { bootstrap, bootstrapHelp } from "../src/bootstrap.js";
import { CommandRunner } from "../src/command-runner.js";

try {
  const options = parseArgs(process.argv.slice(2));
  if (options.help) {
    console.log(bootstrapHelp());
    process.exit(0);
  }
  const exitCode = await bootstrap({
    ...options,
    runner: new CommandRunner({ dryRun: options.dryRun }),
    logger: console
  });
  process.exit(exitCode);
} catch (error) {
  console.error(error.message);
  process.exit(1);
}
