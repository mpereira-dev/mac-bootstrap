{
  "name": "mac-bootstrap",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "Deterministic macOS laptop bootstrap and nightly upkeep.",
  "bin": {
    "mac-bootstrap": "./bin/mac-bootstrap",
    "mac-bootstrap-nightly": "./bin/nightly",
    "mac-bootstrap-doctor": "./bin/doctor",
    "mac-bootstrap-migrate": "./bin/migrate",
    "mac-bootstrap-security": "./bin/security"
  },
  "scripts": {
    "test": "node --test --experimental-test-coverage",
    "test:unit": "node --test test/*.test.js",
    "test:e2e": "node --test test-e2e/*.test.js"
  },
  "engines": {
    "node": ">=24"
  }
}
